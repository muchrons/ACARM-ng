echo -n "checking for XML++ 2.6... "
echo -e "#include <libxml++/libxml++.h>\nint main(void){return 0;}" > "$TMP"
if compile "$TMP" `$SRCS/xml/features/deps/common_dynamic_compile` `$SRCS/xml/features/deps/common_dynamic_link`
then
  echo "ok"
else
  echo "N/A"
  echo "no working ompiler found"
  rm -f "$TMP"
  exit 2
fi
