* add query for getting roots (when dedicated table will be available).
* add view showing meta-alerts currently processed by the system (i.e. elements in use).
* add view showing meta-alerts reported by the system (subset of elements in use).
* add login/logout functionality (note: identification with apache may stay).
* add logging of every action taken in the WUI.
* add logs view table.
* create meta-alert overview, that would show all the meta-alerts' content (i.e.: children nodes) on one page.
* add icon for the 'eventchain' filter in GUI.
* add icon for the 'one2one' filter in GUI.
